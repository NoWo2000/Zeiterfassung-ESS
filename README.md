# Zeiterfassung-ESS


Installation direct:
```bash
python -m pip install -r requirements.txt
```

Installation via venv:
```bash
python -m venv .venv
.\.venv\Scripts\Activate.ps1
python -m pip install -r requirements.txt
```

Usage: 
```bash
python main.pyw
```

Autostart: 
```bash
.\add_to_startup.ps1
```

